# use flake
export XS_PROJECT_ROOT=$(pwd)
export NEMU_HOME=$(pwd)/NEMU
export AM_HOME=$(pwd)/nexus-am
export NOOP_HOME=$(pwd)/XSAI

export DRAMSIM3_HOME=$(pwd)/DRAMsim3

# Linux Kernel 相关环境变量
export RISCV_LINUX_HOME=$(pwd)/linux
export RISCV_ROOTFS_HOME=$(pwd)/riscv-rootfs
export WORKLOAD_BUILD_ENV_HOME=$(pwd)/nemu_board
export OPENSBI_HOME=$(pwd)/opensbi
