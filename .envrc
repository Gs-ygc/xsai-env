# use flake
export XS_PROJECT_ROOT=$(pwd)
export NEMU_HOME=$(pwd)/NEMU
export AM_HOME=$(pwd)/nexus-am
export NOOP_HOME=$(pwd)/XSAI

export DRAMSIM3_HOME=$(pwd)/DRAMsim3

# Linux Kernel 相关环境变量
export RISCV_LINUX_HOME=$(pwd)/linux
export RISCV_ROOTFS_HOME=$(pwd)/riscv-rootfs
export WORKLOAD_BUILD_ENV_HOME=$(pwd)/nemu_board
export OPENSBI_HOME=$(pwd)/opensbi

# RISC-V 工具链环境变量
# export RISCV=/nfs/share/huxuan/riscv-gnu-toolchain-20250906
# export PATH="$RISCV/bin:${PATH}"
# export PATH="/nfs/home/leguochun/verible-v0.0-4023-gc1271a00/bin:${PATH}"

# # 交叉编译工具链和目标架构设置
# export ARCH=riscv
# export CROSS_COMPILE=$RISCV/bin/riscv64-unknown-linux-gnu-
# export GCPT_HOME=$(pwd)/LibCheckpointAlphas
